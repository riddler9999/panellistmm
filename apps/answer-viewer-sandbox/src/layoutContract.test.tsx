import {readFileSync} from 'node:fs';import {describe,expect,it} from 'vitest';describe('mobile layout contract',()=>{it('contains overflow protection without forced page minimum width',()=>{const css=readFileSync(new URL('./styles.css',import.meta.url),'utf8');expect(css).toContain('overflow-wrap: anywhere');expect(css).toContain('min-width: 0');expect(css).not.toMatch(/body\s*\{[^}]*min-width:\s*\d+px/s);expect(css).toContain('.table-scroll');});});
