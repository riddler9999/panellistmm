import {describe,expect,it} from 'vitest';import {getSafeArtifactUrl} from './artifactUrl';describe('artifact URL policy',()=>{it('allows HTTPS only',()=>{expect(getSafeArtifactUrl('https://example.com/a.pdf')).toBe('https://example.com/a.pdf');expect(getSafeArtifactUrl('http://example.com/a')).toBeNull();expect(getSafeArtifactUrl('javascript:alert(1)')).toBeNull();expect(getSafeArtifactUrl('not a url')).toBeNull();});});
