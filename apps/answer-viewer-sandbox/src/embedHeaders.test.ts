import {readFileSync} from 'node:fs';import {describe,expect,it} from 'vitest';describe('embed headers',()=>{it('scopes iframe policy to answer routes',()=>{const config=JSON.parse(readFileSync(new URL('../vercel.json',import.meta.url),'utf8'));const rule=config.headers.find((x:{source:string})=>x.source==='/answer/(.*)');expect(rule).toBeTruthy();const headers=Object.fromEntries(rule.headers.map((h:{key:string,value:string})=>[h.key.toLowerCase(),h.value]));expect(headers['content-security-policy']).toContain('frame-ancestors https:');expect(headers['x-frame-options']).toBeUndefined();expect(headers['x-content-type-options']).toBe('nosniff');expect(headers['referrer-policy']).toBe('no-referrer');});});
