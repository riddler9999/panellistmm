import type {HRArtifact} from '../domain/hrAnswer';import {getSafeArtifactUrl} from '../domain/artifactUrl';const labels={sop:'View Diagram',org_chart:'View Organization Chart',form:'Download Form',file:'Open Attachment'} as const;export function ArtifactCard({artifact}:{artifact:HRArtifact}){const safe=getSafeArtifactUrl(artifact.url);return <article className="artifact-card"><div><span className="artifact-kind">{artifact.type.replace('_',' ')}</span><h3>{artifact.title}</h3></div>{safe?<a className="artifact-action" href={safe} target="_blank" rel="noopener noreferrer">{labels[artifact.type]}</a>:<span className="artifact-unavailable">Link unavailable</span>}</article>}
